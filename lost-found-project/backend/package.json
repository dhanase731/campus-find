{
  "name": "lost-found-backend",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for Lost & Found app",
  "main": "index.js",
  "scripts": {
    "dev": "firebase emulators:start --only auth,functions",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.8.0"
  },
  "engines": {
    "node": "18"
  }
}